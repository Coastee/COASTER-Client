import { css } from "@emotion/react";

export const addButtonStyle = css`
  position: fixed;
  
  bottom: 2rem;
  right: 1rem;

  :hover {
    transition: all 0.1s ease-in-out;
    scale: 1.05;
  }
`;
