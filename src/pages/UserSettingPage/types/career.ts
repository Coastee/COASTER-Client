export interface CareerContentTypes {
  title: string;
  contentList: string[];
  startDate: number[];
  endDate: number[] | null;
}

export interface ExperienceTypes {
  title: string;
  contentList: string[];
  startDate: number[];
  endDate: number[] | null;
}
